name : test artifact 
on : 
  push:
    branches:
    - "master"
jobs:
    test:
      runs-on: ubuntu-22.04
      steps:
        - name : get the code
          uses : actions/checkout
        - name : install python 
          uses : actions/setup-python@v3
          with : 
            python-version: "3.11"

        - name : run the test 
          run  : python3 -m unittest source/main.py

        # - name : Upload Artifacts 
        #   uses : actions/upload-artifact@v3

        #   with :
        #     name: dist-facts
        #     path: dist

    depeloy:
      runs-on: ubunto-22.04
      needs : [test]
      steps:
        - name : Download files needed
          uses : actions/download-artifact@v3
        - name : deployment 
          run  : echo "RD IS DEPELOYING"